import numpy as np
#import 

def f(x):
    return np.pi*a*b

def ellipse_area(f, a=None, b=None)
    semi_major = a
    semi_minor = b
    ellipse_area = ((semi_major/x)**2 + (semi_minor/y)**2)
    return None

if __name__ == "__main__":
    a = 2
    b = 4
    ellipse_area_error = []
    xs = [1000]
    for x in xs;
        number_of_steps = x
        ellipse_area_error.append()
    return None

dist = semi_minor - semi_major
    r = np.linalg.norm(dist)